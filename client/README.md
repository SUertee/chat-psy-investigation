# Client Frontend

This directory contains the browser-side application.

## Structure

- `public/`: static entry pages, vendor files, assets
- `src/`: frontend source code

## Run

From the project root:

```bash
./scripts/run-client.sh
```

Or directly:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
