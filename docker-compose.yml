services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.single
    container_name: ai-psy-app
    env_file:
      - server/.env
    ports:
      - "9000:9000"
    restart: unless-stopped
